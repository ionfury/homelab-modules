# Disable PodSecurityPolicy in the API server
# https://www.talos.dev/v1.9/reference/configuration/v1alpha1/config/#Config.cluster.apiServer
---
%{ if type == "controlplane" }
cluster:
  apiServer:
    disablePodSecurityPolicy: true
%{ endif }